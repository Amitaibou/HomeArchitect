events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index landing.html;
        }

        location /index.html {
            root /usr/share/nginx/html;
        }

        location /api {
            proxy_pass http://app:8000;
        }
    }
}